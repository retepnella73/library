<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <button type="button" class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="brand" href="#">Shelved</a>
      <div class="nav-collapse collapse">
        <ul class="nav">
          <!--li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              Books
              <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to 'Books', books_path %></li>
              <li class="divider"></li>
              <li><%= link_to 'Publishers', publishers_path %></li>
              <li><%= link_to 'Authors', authors_path %></li>
            </ul>
          </li-->
          <%= nav_item 'Books', books_path %>
          <%= nav_item 'Music', discs_path %>
        </ul>
        <ul class="nav pull-right">
          <% if user_signed_in? %>
            <li class="navbar-text muted"><em>Logged in as</em> <%= current_user.username %></li>
            <li><%= link_to 'Log Out', destroy_user_session_path, :method => 'delete' %></li>
          <% else %>
            <!--li><%= link_to 'Sign Up', new_user_registration_path %></li-->
            <%= nav_item 'Log In', new_user_session_path %>
          <% end %>
        </ul>
      </div>
    </div>
  </div>
</div>

